@model AnnouncementUpdateDto
@{
    ViewData["Title"] = "UpdateAnnouncement";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">Duyuru Güncelleme Sayfası</h3>
        </div>
        <div class="card-body p-4">
            <form method="post">
                <input type="hidden" asp-for="AnnouncementID" />

                <div class="mb-4">
                    <label for="title" class="form-label fw-bold">Duyuru Başlığı</label>
                    <input type="text" asp-for="Title" class="form-control" id="title" placeholder="Duyuru başlığını giriniz" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label for="content" class="form-label fw-bold">Duyuru İçeriği</label>
                    <textarea asp-for="Content" class="form-control" id="content" placeholder="Duyuru içeriğini giriniz" rows="5"></textarea>
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>

                <div class="text-end">
                    <button type="submit" class="btn btn-primary px-4">
                        <i class="bi bi-pencil-square"></i> Duyuruyu Güncelle
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
